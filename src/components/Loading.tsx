const Loading = () => {
    return (
        <div className="h-screen w-full flex justify-center items-center font-semibold text-lg">
            Loading...
        </div>
    )
}

export default Loading
